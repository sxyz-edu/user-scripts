language: node_js
node_js:
  - node
install:
  - npm install
script:
  # check grammar
  - npm run eslint
  - npm run stylelint
  # check if files in `dist/` is up-to-date
  - mv dist _dist
  - npm run build
  - diff dist _dist
